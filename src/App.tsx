import { FC } from "react";
import Layout from "./components/Layout";
import Table from "./components/Table/Table";

const App: FC = () => {
  return (
    <Layout>
      <Table />
    </Layout>
  );
};

export default App;
